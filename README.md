# WebSocket Load Testing Client

## 📌 Overview
Данный проект представляет собой **клиент для нагрузочного тестирования WebSocket‑серверов**, написанный на Go.

Клиент устанавливает множество WebSocket‑соединений к одному или нескольким серверам и **периодически отправляет сообщения**, имитируя поведение активных пользователей чата.

Проект предназначен для:
- тестирования устойчивости WebSocket‑серверов
- проверки работы TLS (WSS)
- анализа поведения сервера под параллельной нагрузкой

---

## 🎯 Назначение

Клиент используется совместно с WebSocket‑чатом и позволяет:
- создавать заданное количество соединений к каждому хосту
- отправлять сообщения с фиксированным интервалом
- корректно завершать работу по сигналам ОС (SIGINT / SIGTERM)

---

## ⚙️ Основные возможности

- Поддержка **нескольких WebSocket‑серверов** одновременно
- Настраиваемое количество соединений на хост
- Работа через **WSS (TLS)**
- Асинхронная отправка сообщений
- Graceful shutdown через `context.Context`
- Формирование JSON‑сообщений

---

## 🧱 Архитектура

Клиент построен по конкурентной модели Go:

- `errgroup.Group` — управление жизненным циклом горутин
- `context.Context` — централизованная отмена работы
- `gorilla/websocket` — WebSocket‑клиент

### Поток выполнения

```
main
 ├─ parse flags
 ├─ create context + errgroup
 ├─ for each host
 │   └─ create N websocket connections
 │       └─ goroutine sendMessages()
 └─ wait for shutdown signal
```

---

## 📡 Формат сообщений

Каждое сообщение отправляется в формате JSON:

```json
{
  "address": "192.168.1.10",
  "message": "2024-01-01T12:00:00Z Hello",
  "time": "12:00",
  "host": "load-client-host"
}
```

### Поля
| Поле | Описание |
|-----|---------|
| `address` | IP‑адрес машины клиента |
| `message` | Текст сообщения |
| `time` | Время отправки |
| `host` | Имя хоста клиента |

---

## 🚀 Запуск

### Сборка и запуск

```bash
go run main.go
```

---

## 🧩 Параметры командной строки

| Флаг | Описание | Значение по умолчанию |
|-----|---------|-----------------------|
| `-h` | Список адресов WebSocket‑серверов (через `;`) | `localhost:8443;localhost:8444` |
| `-c` | Количество соединений на каждый сервер | `25` |

### Пример

```bash
go run main.go -a "localhost:8443;localhost:8444" -c 100
```

В примере:
- подключение к двум серверам
- по 100 WebSocket‑соединений к каждому

---

## 🔐 TLS / WSS

Клиент использует `wss://` и поддерживает self‑signed сертификаты:

```go
TLSClientConfig: &tls.Config{
    InsecureSkipVerify: true,
}
```

⚠️ **Важно:** данный режим рекомендуется только для тестирования.

---

## 🛑 Завершение работы

Клиент корректно обрабатывает сигналы:
- `SIGINT`
- `SIGTERM`

При завершении:
- отправляется `CloseMessage`
- все соединения закрываются
- горутины завершаются через `context` и `errgroup`

---

## 🛠 Используемые технологии

- Go
- Gorilla WebSocket
- TLS / WSS
- logrus
- errgroup

---
